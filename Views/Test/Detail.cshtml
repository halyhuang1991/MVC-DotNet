 @section SubMenu { Hello This is a section implement in About View. } @{ ViewData["Title"] = "Home Page"; } @using System @using MVC_DotNet.Models
<div class="container" id="part">
    <table class="table table-striped table-hover" data-striped="true" data-height="500" data-toggle="table">
        <thead>
            <tr>
                <th>id</th>
                <th>name</th>
            </tr>
        </thead>
        <tbody>
            @if(Model!=null){
            <tr>
                <td>@Model.id</td>
                <td>@Model.name</td>
            </tr>
            }
            <tr>
                <td></td>
            </tr>
        </tbody>
    </table>
</div>

<p>
    @Html.ActionLink("Create New", "DetailAdd")
</p>
<script type="text/javascript">
    $(function() {

    })
</script>